frontend - npm run dev  (from cd frontend)
backend - uvicorn backend.main:app --reload  (from root)
backend -  uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000

to build the frontend package - npx create-next-app@latest

When /docs opens but fetch() fails from the browser, it is almost always CORS.